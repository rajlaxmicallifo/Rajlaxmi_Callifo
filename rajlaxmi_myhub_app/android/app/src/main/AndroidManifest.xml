<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.rajlaxmi_myhub_app">

    <!-- =============================== -->
    <!-- ✅ Core Permissions -->
    <!-- =============================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- =============================== -->
    <!-- ⚠️ Call Recording / Audio Permissions -->
    <!-- =============================== -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />

    <!-- =============================== -->
    <!-- ✅ Storage Permissions (Local Recordings) -->
    <!-- =============================== -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- =============================== -->
    <!-- ✅ Foreground Service -->
    <!-- =============================== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- =============================== -->
    <!-- Queries for phone intents -->
    <!-- =============================== -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PHONE_STATE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
        </intent>
    </queries>

    <!-- =============================== -->
    <!-- Application -->
    <!-- =============================== -->
    <application
        android:label="Rajlaxmi MyHub"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <!-- =============================== -->
        <!-- Main Activity -->
        <!-- =============================== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
        </activity>

        <!-- =============================== -->
        <!-- Flutter embedding version -->
        <!-- =============================== -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- =============================== -->
        <!-- Optional Foreground service -->
        <!-- =============================== -->
        <service
            android:name="com.shounakmulay.flutter_phone_call_state.CallStateService"
            android:permission="android.permission.FOREGROUND_SERVICE"
            android:exported="false" />

        <!-- =============================== -->
        <!-- ⚠️ Removed PhoneCallStateReceiver to prevent crash -->
        <!-- If you install the plugin flutter_phone_call_state, re-add this receiver -->
        <!-- =============================== -->

    </application>
</manifest>